#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <fstream>
#include <stdio.h>

void readNumbers(int size, int* digArr);
long long int checkProduct(int prodSize, int* digArr, int size);

#endif // FUNCTIONS_H
